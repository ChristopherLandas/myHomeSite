import styles from '../styles/ParallaxTitle.module.css'

const ParallaxTitle = () => {
  return (
    <div className={styles.root}>
      <div className={styles.workableArea}>
        a
      </div>
    </div>
  )
}

export default ParallaxTitle